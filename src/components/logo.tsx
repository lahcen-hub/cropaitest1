import { Leaf } from 'lucide-react';

export const Logo = () => (
  <div className="flex items-center gap-2">
    <Leaf className="h-7 w-7 text-primary" />
    <h1 className="text-2xl font-bold text-primary font-headline">CropAI</h1>
  </div>
);
